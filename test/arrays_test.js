const assert = require('assert');
const myArray = require('../src/arrays.js');

describe('arrayFunctions', function() {
  describe('.add()', function() {
    it('should add an element to the end of an array', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
    it('should adjust the length of the array', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
  });

  describe('.remove()', function() {
    it('should remove and return the last element in the array', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
    it('should adjust the length of the array', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
  });

  describe('.indexOf()', function() {
    it('should return the correct index when the value is present', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
    it('should return -1 when the value is not present', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
  });

  describe('.contains()', function() {
    it('should return true if the element is found', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
    it('should return false if the value is not found', function() {
      // setup

      // exercise

      // verfify
      //assert.equal([1,2,3].indexOf(4), -1);
  });
  });

  describe('.flatten()', function() {
    it('should return a new array with the sub-array elements concatted into it.', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
  }); 

  describe('.reverse()', function() {
    it('should return the array in reverse order', function() {
        // setup

        // exercise

        // verfify
        //assert.equal([1,2,3].indexOf(4), -1);
    });
  });

  describe('.toString()', function() {
    it('should return a string representation of the array', function() {
        // setup

        // exercise

        //assert.equal([1,2,3].indexOf(4), -1);
    });
  });
});